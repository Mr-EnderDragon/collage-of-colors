# Collage of Colours

A modern React + Vite portfolio / landing page for a creative print and branding studio. `Collage of Colors` showcases animated product cards, custom brand visuals, and a polished responsive hero experience built with Tailwind CSS and motion animations.

## 🌈 Project Overview

This project is a frontend presentation site with:
- A bold hero section and animated gallery cards
- A responsive navigation header
- Interactive hover motion effects using `motion`
- A lightweight, modern UI built with Tailwind CSS
- A fully TypeScript-powered React application

## 🧩 Tech Stack

- React 19
- TypeScript
- Vite
- Tailwind CSS
- Framer Motion / Motion
- Radix UI primitives
- Lucide icons

## 🚀 Getting Started

### Prerequisites

- Node.js 18+ installed
- npm or yarn available

### Install dependencies

```bash
npm install
```

### Run locally

```bash
npm run dev
```

Open the local development URL shown in the terminal to preview the site.

### Build for production

```bash
npm run build
```

### Preview production build

```bash
npm run preview
```

## 📁 Project Structure

- `src/`
  - `App.tsx` — main home page layout and animated hero experience
  - `main.tsx` — app entry point
  - `assets/` — image assets used by the UI
  - `components/` — reusable UI components
  - `lib/utils.ts` — helper utilities
  - `styles/globals.css` — global styles and Tailwind imports
- `package.json` — scripts and dependencies
- `vite.config.ts` — Vite configuration
- `tailwind.config.ts` — Tailwind CSS configuration

## ✨ Key Features

- Animated `BounceCards` hero gallery
- Hover interactions with motion transitions
- Clean responsive layout and glassmorphism-style header
- Reusable button and UI components
- Smooth text and card entrance animations

## 🛠️ Available Scripts

- `npm run dev` — start the development server
- `npm run build` — compile production assets
- `npm run preview` — preview the production build locally
- `npm run lint` — run ESLint

## 💡 Notes

This repository is configured as a private project with modern frontend tooling and a focus on visual presentation. Customize the branding, images, and content to adapt it for any creative studio, packaging, or print service.